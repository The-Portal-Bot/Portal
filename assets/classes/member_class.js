module.exports = class {
	constructor(level, rank, tier, points, timestamp) {
		this.level = level;
		this.rank = rank;
		this.tier = tier;
		this.points = points;

		this.timestamp = timestamp;
	}
};